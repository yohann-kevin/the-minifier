const manageDistOptions = () => {
  // TODO: manage dist options
};

const manageLanguageOptions = (languageSelected) => {
  // TODO: manage language options
  console.log(languageSelected);
};

const manageExcludeOptions = (filesExclude) => {
  // TODO: manage exclude options
  console.log(filesExclude);
};

module.exports = {
  manageDistOptions,
  manageLanguageOptions,
  manageExcludeOptions,
};
