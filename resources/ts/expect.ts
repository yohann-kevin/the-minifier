// plop

/**
 *
 * @param {number} a
 * @param {number} b
 * @returns
 */
 function mutliply(a: number, b: number) {
  return a * b; // return a multiply by b
}

/**
*
* @param {number} a
* @param {number} i
* @returns
*/
function increment(a: number, i: number) {
  if (i === a) {
    // return i multpiply a
    return i * a;
  }
  return a += i;
}

/* plop msg */

/**
 * 
 * @param {number} a 
 * @param {number} b 
 * @returns 
 */
function substract(a: number, b: number) {
  return a - b;
}
